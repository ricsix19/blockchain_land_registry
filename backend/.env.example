# Server
PORT=3000
NODE_ENV=development

# PostgreSQL (connection string)
DATABASE_URL=postgres://user:password@localhost:5432/land_registry

# Auth (set JWT_SECRET in any shared environment)
JWT_SECRET=change-me-in-production
